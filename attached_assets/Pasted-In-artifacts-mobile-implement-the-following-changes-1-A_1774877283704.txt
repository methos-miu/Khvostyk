In artifacts/mobile, implement the following changes:
	1.	Add fields length (number), height (number), personality (string), description (string) to the Pet interface in context/PetsContext.tsx. Add bloodType (string) and illnesses (array of objects with fields: id, name, startDate, endDate, description) to the MedicalProfile interface. Update assemblePets, migratePet, addPet, and updatePet functions to include all new fields. Fix updatePet so it never overwrites existing pet fields with null — before calling Supabase update, merge the existing pet data with updates so all fields are always present.
	2.	In app/pet/add.tsx and app/pet/edit/[id].tsx, add drum-roll pickers for length and height (1–200 cm, step 1, same style as the existing weight picker), and text inputs for personality and description. When a pet is added with a weight, automatically create the first entry in weightHistory for today’s date. When weight is edited, if today already has an entry in weightHistory overwrite it, otherwise create a new one. The weight field on the pet always reflects the latest weightHistory entry.
	3.	In app/pet/[id].tsx, add a section between “Details” and “Medical Profile” showing personality and description. Show length and height in the Details section alongside weight. In the Medical Profile modal, add a bloodType selector (dropdown or chip list with all standard blood types for animals: A, B, AB, DEA 1.1+, DEA 1.2+, DEA 3, DEA 4, DEA 5, DEA 7, A/B etc.) and an illnesses list with ability to add, edit and delete entries — each entry has name, start date, end date, and description fields with date pickers. Remove numberOfLines limit from allergies and chronicConditions display fields so all text is visible.
	4.	In app/pet/weight/[id].tsx, allow editing the weight value of existing entries but not the date. Ensure that if two entries exist for the same date only one is kept (the latest value).
	5.	In Supabase, add columns length, height, personality, description to the pets table. The medical_profile JSON column already exists and will now store bloodType and illnesses as additional fields.
	6.	All new UI text must support both Ukrainian and English using the existing language === "uk" ? "..." : "..." pattern. No other changes.​​​​​​​​​​​​​​​​